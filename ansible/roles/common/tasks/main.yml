---
- name: Update apt cache and upgrade packages
  apt:
    update_cache: yes
    upgrade: safe

- name: Install base utilities
  apt:
    name:
      - curl
      - git
      - ufw
      - unzip
    state: present
